<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="purchaseHistory" type="com.example.hishab.models.PurchaseHistory"/>
    </data>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/tv_date"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginLeft="8dp"
        android:lines="2"
        android:textAlignment="center"
        android:text="@{purchaseHistory.dateTimeString}"
        />
    <TextView
        android:id="@+id/tv_purchase_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@{purchaseHistory.itemName}"
        app:layout_constraintLeft_toRightOf="@id/tv_date"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginLeft="8dp"

        />
    <TextView
        android:id="@+id/tv_category"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/tv_date"
        app:layout_constraintTop_toBottomOf="@+id/tv_purchase_item_name"
        android:text="@{purchaseHistory.categoryName}"
        android:layout_marginLeft="8dp"
        />
    <TextView
        android:id="@+id/tv_cost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginRight="8dp"
        android:layout_marginTop="8dp"
        android:text="@{``+ purchaseHistory.cost}"
        />
    <TextView
        android:id="@+id/tv_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/tv_category"
        app:layout_constraintTop_toBottomOf="@id/tv_cost"
        app:layout_constraintHorizontal_bias="0.9"
        android:layout_marginRight="8dp"
        android:text="@{purchaseHistory.descipt}"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
</layout>